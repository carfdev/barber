---
import Background from "@assets/bg-blur-balls.svg";
import HeroImage from "@assets/hero-image.webp";
import { Image } from "astro:assets";
---

<section
  id="hero"
  class="relative flex h-dvh items-center overflow-hidden pt-20"
>
  <div
    class="from-brand-dark to-brand-dark-secondary absolute inset-0 z-0 bg-gradient-to-b"
  >
    <Image
      class="h-full w-full object-cover opacity-20"
      src={Background}
      alt="Background with blurred balls"
      loading="eager"
    />
  </div>
  <div
    class="relative z-10 container mx-auto flex h-full items-center justify-between px-4"
  >
    <div class="max-w-3xl">
      <h2
        class="font-montserrat text-brand-gold mb-6 text-4xl leading-tight font-bold tracking-wider uppercase md:text-5xl lg:text-6xl"
      >
        Din Stil,<br />Min Hantverk.
      </h2>
      <p class="mb-8 max-w-2xl text-xl text-white">
        För den moderna mannen som värdesätter precision och kvalitet. Jag
        kombinerar tidlösa tekniker med aktuella trender för en stil som håller.
      </p>
      <div class="flex flex-wrap gap-4">
        <a
          href="#contact-main"
          class="bg-brand-gold text-brand-dark font-montserrat hover:text-brand-gold cursor-pointer rounded-lg px-8 py-3 text-base font-semibold tracking-wider uppercase transition duration-300 hover:bg-white"
          >Kontakta Mig</a
        >
        <a
          href="#portfolio-grid"
          class="border-brand-gold text-brand-gold font-montserrat hover:bg-brand-gold hover:text-brand-dark cursor-pointer rounded-lg border-2 px-8 py-3 text-base font-semibold tracking-wider uppercase transition duration-300"
          >Se min galleri</a
        >
      </div>
    </div>
    <div class="hidden xl:block">
      <Image
        class="mask-image h-[800px] w-auto rounded-lg object-cover shadow-lg"
        src={HeroImage}
        alt="A barber"
        loading="eager"
      />
    </div>
  </div>
</section>

<style>
  .mask-image {
    mask-image: linear-gradient(black 65%, transparent 85%);
  }
</style>
